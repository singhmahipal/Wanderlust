<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust</title>
</head>
<body>
    <h3>Edit Listing</h3>
    <form method="post" action="/listings/<%= listing._id %>?_method=PUT">
        <input type="text" name="listing[title]" placeholder="<%= listing.title %>" value="<%= listing.title %>" />
        <br><br>
        <textarea name="listing[description]" placeholder="<%= listing.description %>"><%= listing.description %></textarea>
        <br><br>
        <input type="text" name="listing[image]" placeholder="<%= listing.image.url %>" value="<%= listing.image.url %>" />
        <br><br>
        <input type="number" name="listing[price]" placeholder="<%= listing.price %>" value="<%= listing.price %>" />
        <br><br>
        <input type="text" name="listing[location]" placeholder="<%= listing.location %>" value="<%= listing.location %>" />
        <br><br>
        <input type="text" name="listing[country]" placeholder="<%= listing.country %>" value="<%= listing.country %>" />
        <br><br>
        <button>Edit</button>
    </form>
</body>
</html>